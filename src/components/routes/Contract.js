import { useContext, useRef, useState } from "react";
// import {useVarsContext} from "../../contexts/VarsContext"

export default function Contract() {
  // const {conAddr, setConAddr} = useVarsContext()

  return (
    <>
      In Contract route
    </>
  )
}